# Ignore everything by default
*

# Allow the txData/resources folder and its contents, but ignore .json files
!/txData/
!/txData/resources/
!/txData/resources/**

# Ignore all .json files in txData/resources and subdirectories
txData/resources/**/*.json

# Ignore the server folder and everything inside it
server/

# Ignore the txData/cache folder and everything inside it
txData/cache/

# Ignore the txData/default folder and everything inside it
txData/default/